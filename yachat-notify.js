(()=>{const modal=()=>document.querySelector('#chatModal'),messages=()=>document.querySelector('#messages');let last=0;function notify(){if(!('Notification'in window)||Notification.permission!=="granted")return;if(Date.now()-last<1200)return;last=Date.now();new Notification('yaprxy. — new message',{body:'You have a new chat message',icon:'/favicon.ico'})}const observer=new MutationObserver(()=>{const m=messages();if(!m||modal()?.classList.contains('show'))return;notify()});const start=()=>{const m=messages();if(m)observer.observe(m,{childList:true});};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();})();
